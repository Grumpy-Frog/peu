//C Program to Calculate Area of Square
#include<stdio.h>
int main ()
{
    float a, area;

    printf("Length of Square:");
    scanf("%f",&a);

    area = (a*a);

    printf("Area of the square:%f", area);

    return 0;
}
